request and addreeses for repo transfer of these emails to you by this week and addreeses ```
npm i
node index.js -o https://original.url -t http://test.url -e "endpoint?apiKey=freekey&debugId=click" -m GET
```